{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "asRoot": true,
            "console": "integratedTerminal",
            "program": "./",  // 调试当前文件所在的目录（包）
            "args": [],                   // 如果你的程序需要启动参数，写在这里
            "env": {},                    // 环境变量
            "showLog": true
        }
    ]
}